# Instructions
1 - move your python file into the main directory for this repository<br>
2 - Create a virtual environment. You will need to install a dependency once your virtual environment is activated by doing ```$ python -m pip install prosperity4btx```<br>
3 - To run the backtester, do ```python -m prosperity4bt [path to algorithm file] 0```<br>
